# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/

# Skills de IA (ui-ux-pro-max, emil design/animation skills) — herramientas
# locales para el asistente, no forman parte del sitio.
.agents/
skills-lock.json

# Secretos locales (API keys, etc.) — nunca a git. .mcp.json sí se commitea,
# pero solo referencia variables de entorno (${VAR}), nunca el valor real.
.env
.env.*
!.env.example
